# Docker Compose 环境变量（复制为仓库根目录 .env）
#   copy docker.env.example .env
#
# 修改密码后需同步到 apps/server/.env 的 DB_PASSWORD、REDIS_PASSWORD

POSTGRES_PASSWORD=change-me-postgres-password
REDIS_PASSWORD=change-me-redis-password
