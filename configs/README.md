# configs

本目录用于存放 **跨 workspace 复用** 的工程化配置与规范文件，例如：

- `tsconfig` 基础配置
- ESLint / Prettier 规则说明（如需）
- 环境变量约定（如需）

说明：本项目目前将核心配置放在仓库根目录（`tsconfig.base.json`、`eslint.config.mjs`、`prettier.config.cjs`）。

