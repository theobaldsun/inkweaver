---
description: SyncBox-AI Monorepo 架构、应用分层与依赖边界
alwaysApply: true
---

# Monorepo 架构约束

产品：InkWeaver（Local-First 跨端笔记）。包 scope：`@inkweaver/*`。详情见 `docs/SYSTEM_DESIGN.md`。

## 应用层（apps/）

| 路径 | 职责 | 技术栈 |
|------|------|--------|
| `apps/server` | REST、WebSocket 同步、用户/文档/搜索/存储/AI | NestJS, TypeORM, PostgreSQL, Redis/Bull, Socket.io |
| `apps/web` | PC Web 客户端 | Vite, React 19, Yjs, Dexie |
| `apps/h5` | 移动 Web | 同 web |
| `apps/mobile` | 原生 App | Expo ~55, RN 0.83 |
| `apps/admin` | 管理后台 | Vite, React 19 |

## 共享包（packages/）

`shared` · `api` · `services` · `platform` · `adapters` · `db-adapter` · `sync-engine` · `editor-core` · `editor-web` · `editor-mobile` · `ui` · `assets`

## 依赖边界（必须遵守）

- `apps` 只依赖 `packages`，**app 之间不互相引用**
- `packages/shared` 不依赖任何 app
- `packages/sync-engine` 仅依赖 `shared`
- HTTP 客户端与 API 定义：`packages/api`
- 业务逻辑：`packages/services`
- `packages/ui` 不依赖具体业务

## 核心模式

本地优先（Dexie / SQLite）+ Yjs CRDT（`sync-engine`）+ Nest 云端协调。

## 常用命令

```bash
pnpm dev:server   # 后端
pnpm dev:web      # PC Web
pnpm dev:h5       # H5
pnpm dev:mobile   # Expo
pnpm build && pnpm typecheck && pnpm lint
```
